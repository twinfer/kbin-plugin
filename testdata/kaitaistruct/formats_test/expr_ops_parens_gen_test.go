// Code generated by kaitai-test-gen-simple.go; DO NOT EDIT.
package formats_test

import (
	"bytes"
	"context"
	"log/slog"
	"maps"
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/google/go-cmp/cmp/cmpopts"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/twinfer/kbin-plugin/pkg/kaitaistruct"
	expr_ops_parens_kaitai "github.com/twinfer/kbin-plugin/testdata/formats_kaitai_go_gen/expr_ops_parens"
)

func TestParse_ExprOpsParens(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "expr_ops_parens.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)
	logger := slog.New(slog.NewTextHandler(os.Stderr, &slog.HandlerOptions{Level: slog.LevelDebug}))
	interpreter, err := kaitaistruct.NewKaitaiInterpreter(schema, logger)
	require.NoError(t, err)

	// Read binary file
	binPath := filepath.Join("../../../test/src", "enum_negative.bin")
	binData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse with custom parser
	stream := kaitai.NewStream(bytes.NewReader(binData))
	parsed, err := interpreter.Parse(context.Background(), stream)
	require.NoError(t, err)

	// Convert to map for assertions
	customMap := kaitaistruct.ParsedDataToMap(parsed).(map[string]any)

	// Assertions from KSC test

	// Assert ISumToStr
	if isumtostr_val, ok := customMap["ISumToStr"]; ok {
		assert.EqualValues(t, "29", isumtostr_val)
	} else {
		t.Fatalf("Field 'ISumToStr' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert FSumToInt
	if fsumtoint_val, ok := customMap["FSumToInt"]; ok {
		assert.EqualValues(t, 9, fsumtoint_val)
	} else {
		t.Fatalf("Field 'FSumToInt' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrConcatLen
	if strconcatlen_val, ok := customMap["StrConcatLen"]; ok {
		assert.EqualValues(t, 10, strconcatlen_val)
	} else {
		t.Fatalf("Field 'StrConcatLen' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrConcatRev
	if strconcatrev_val, ok := customMap["StrConcatRev"]; ok {
		assert.EqualValues(t, "9876543210", strconcatrev_val)
	} else {
		t.Fatalf("Field 'StrConcatRev' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrConcatSubstr2To7
	if strconcatsubstr2to7_val, ok := customMap["StrConcatSubstr2To7"]; ok {
		assert.EqualValues(t, "23456", strconcatsubstr2to7_val)
	} else {
		t.Fatalf("Field 'StrConcatSubstr2To7' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrConcatToI
	if strconcattoi_val, ok := customMap["StrConcatToI"]; ok {
		assert.EqualValues(t, 123456789, strconcattoi_val)
	} else {
		t.Fatalf("Field 'StrConcatToI' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert BoolEq
	if booleq_val, ok := customMap["BoolEq"]; ok {
		assert.EqualValues(t, 0, booleq_val)
	} else {
		t.Fatalf("Field 'BoolEq' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert BoolAnd
	if booland_val, ok := customMap["BoolAnd"]; ok {
		assert.EqualValues(t, 0, booland_val)
	} else {
		t.Fatalf("Field 'BoolAnd' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert BoolOr
	if boolor_val, ok := customMap["BoolOr"]; ok {
		assert.EqualValues(t, 1, boolor_val)
	} else {
		t.Fatalf("Field 'BoolOr' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

}

func TestSerialize_ExprOpsParens(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "expr_ops_parens.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)

	serializer, err := kaitaistruct.NewKaitaiSerializer(schema, nil)
	require.NoError(t, err)

	// Read original binary
	binPath := filepath.Join("../../../test/src", "enum_negative.bin")
	originalData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse original
	stream := kaitai.NewStream(bytes.NewReader(originalData))
	original := expr_ops_parens_kaitai.NewExprOpsParens()
	err = original.Read(stream, original, original)
	require.NoError(t, err)

	// Serialize and verify round-trip
	inputMap := structToMapExprOpsParens(t, original)

	serialized, err := serializer.Serialize(context.Background(), inputMap)
	require.NoError(t, err)

	// Parse serialized data
	stream2 := kaitai.NewStream(bytes.NewReader(serialized))
	reparsed := expr_ops_parens_kaitai.NewExprOpsParens()
	err = reparsed.Read(stream2, reparsed, reparsed)
	require.NoError(t, err)

	// Compare structures with Phase 2/3 enhanced comparison
	cmpOpts := []cmp.Option{
		cmpopts.IgnoreUnexported(
			kaitai.Stream{},
		),
		cmpopts.IgnoreFields(expr_ops_parens_kaitai.ExprOpsParens{}, "_io", "_parent", "_root"),
		cmpopts.EquateEmpty(),
	}
	if diff := cmp.Diff(original, reparsed, cmpOpts...); diff != "" {
		t.Errorf("Serialization mismatch (-original +reparsed):\n%s", diff)
	}
}

// structToMapExprOpsParens converts KSC struct to map for serializer
func structToMapExprOpsParens(t *testing.T, data any) map[string]any {
	// Use reflection to handle KSC structs with method-based values
	return structToMapReflectiveExprOpsParens(t, data)
}

// structToMapReflectiveExprOpsParens uses reflection to convert KSC structs including method calls
func structToMapReflectiveExprOpsParens(t *testing.T, data any) map[string]any {
	result := make(map[string]any)

	val := reflect.ValueOf(data)
	if val.Kind() == reflect.Ptr {
		val = val.Elem()
	}

	typ := val.Type()

	// Process fields
	for i := 0; i < val.NumField(); i++ {
		field := typ.Field(i)
		fieldVal := val.Field(i)

		// Skip internal KSC fields
		if strings.HasPrefix(field.Name, "_") {
			continue
		}

		fieldName := strings.ToLower(field.Name)

		if !fieldVal.IsValid() || fieldVal.IsZero() {
			continue
		}

		// Handle different field types
		switch fieldVal.Kind() {
		case reflect.Ptr:
			if !fieldVal.IsNil() {
				// For custom types, check for AsInt/AsStr methods
				subResult := make(map[string]any)

				// Try to call AsInt method
				if method := fieldVal.MethodByName("AsInt"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asint"] = results[0].Interface()
					}
				}

				// Try to call AsStr method
				if method := fieldVal.MethodByName("AsStr"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asstr"] = results[0].Interface()
					}
				}

				// Also include raw fields
				subMap := structToMapReflectiveExprOpsParens(t, fieldVal.Interface())
				for k, v := range subMap {
					if _, exists := subResult[k]; !exists {
						subResult[k] = v
					}
				}

				result[fieldName] = subResult
			}
		case reflect.Slice:
			if fieldVal.Len() > 0 {
				slice := make([]any, fieldVal.Len())
				for j := 0; j < fieldVal.Len(); j++ {
					slice[j] = fieldVal.Index(j).Interface()
				}
				result[fieldName] = slice
			}
		default:
			result[fieldName] = fieldVal.Interface()
		}
	}

	return result
}
