// Code generated by kaitai-test-gen-simple.go; DO NOT EDIT.
package formats_test

import (
	"bytes"
	"context"
	"log/slog"
	"maps"
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/google/go-cmp/cmp/cmpopts"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/twinfer/kbin-plugin/pkg/kaitaistruct"
	expr_calc_array_ops_kaitai "github.com/twinfer/kbin-plugin/testdata/formats_kaitai_go_gen/expr_calc_array_ops"
)

func TestParse_ExprCalcArrayOps(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "expr_calc_array_ops.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)
	logger := slog.New(slog.NewTextHandler(os.Stderr, &slog.HandlerOptions{Level: slog.LevelDebug}))
	interpreter, err := kaitaistruct.NewKaitaiInterpreter(schema, logger)
	require.NoError(t, err)

	// Read binary file
	binPath := filepath.Join("../../../test/src", "fixed_struct.bin")
	binData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse with custom parser
	stream := kaitai.NewStream(bytes.NewReader(binData))
	parsed, err := interpreter.Parse(context.Background(), stream)
	require.NoError(t, err)

	// Convert to map for assertions
	customMap := kaitaistruct.ParsedDataToMap(parsed).(map[string]any)

	// Assertions from KSC test

	// Assert IntArraySize
	if intarraysize_val, ok := customMap["IntArraySize"]; ok {
		assert.EqualValues(t, 7, intarraysize_val)
	} else {
		t.Fatalf("Field 'IntArraySize' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert IntArrayFirst
	if intarrayfirst_val, ok := customMap["IntArrayFirst"]; ok {
		assert.EqualValues(t, 10, intarrayfirst_val)
	} else {
		t.Fatalf("Field 'IntArrayFirst' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert IntArrayMid
	if intarraymid_val, ok := customMap["IntArrayMid"]; ok {
		assert.EqualValues(t, 25, intarraymid_val)
	} else {
		t.Fatalf("Field 'IntArrayMid' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert IntArrayLast
	if intarraylast_val, ok := customMap["IntArrayLast"]; ok {
		assert.EqualValues(t, 1000, intarraylast_val)
	} else {
		t.Fatalf("Field 'IntArrayLast' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert IntArrayMin
	if intarraymin_val, ok := customMap["IntArrayMin"]; ok {
		assert.EqualValues(t, 10, intarraymin_val)
	} else {
		t.Fatalf("Field 'IntArrayMin' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert IntArrayMax
	if intarraymax_val, ok := customMap["IntArrayMax"]; ok {
		assert.EqualValues(t, 1000, intarraymax_val)
	} else {
		t.Fatalf("Field 'IntArrayMax' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert DoubleArraySize
	if doublearraysize_val, ok := customMap["DoubleArraySize"]; ok {
		assert.EqualValues(t, 5, doublearraysize_val)
	} else {
		t.Fatalf("Field 'DoubleArraySize' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArraySize
	if strarraysize_val, ok := customMap["StrArraySize"]; ok {
		assert.EqualValues(t, 4, strarraysize_val)
	} else {
		t.Fatalf("Field 'StrArraySize' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArrayFirst
	if strarrayfirst_val, ok := customMap["StrArrayFirst"]; ok {
		assert.EqualValues(t, "un", strarrayfirst_val)
	} else {
		t.Fatalf("Field 'StrArrayFirst' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArrayMid
	if strarraymid_val, ok := customMap["StrArrayMid"]; ok {
		assert.EqualValues(t, "deux", strarraymid_val)
	} else {
		t.Fatalf("Field 'StrArrayMid' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArrayLast
	if strarraylast_val, ok := customMap["StrArrayLast"]; ok {
		assert.EqualValues(t, "quatre", strarraylast_val)
	} else {
		t.Fatalf("Field 'StrArrayLast' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArrayMin
	if strarraymin_val, ok := customMap["StrArrayMin"]; ok {
		assert.EqualValues(t, "deux", strarraymin_val)
	} else {
		t.Fatalf("Field 'StrArrayMin' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert StrArrayMax
	if strarraymax_val, ok := customMap["StrArrayMax"]; ok {
		assert.EqualValues(t, "un", strarraymax_val)
	} else {
		t.Fatalf("Field 'StrArrayMax' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

}

func TestSerialize_ExprCalcArrayOps(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "expr_calc_array_ops.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)

	serializer, err := kaitaistruct.NewKaitaiSerializer(schema, nil)
	require.NoError(t, err)

	// Read original binary
	binPath := filepath.Join("../../../test/src", "fixed_struct.bin")
	originalData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse original
	stream := kaitai.NewStream(bytes.NewReader(originalData))
	original := expr_calc_array_ops_kaitai.NewExprCalcArrayOps()
	err = original.Read(stream, original, original)
	require.NoError(t, err)

	// Serialize and verify round-trip
	inputMap := structToMapExprCalcArrayOps(t, original)

	serialized, err := serializer.Serialize(context.Background(), inputMap)
	require.NoError(t, err)

	// Parse serialized data
	stream2 := kaitai.NewStream(bytes.NewReader(serialized))
	reparsed := expr_calc_array_ops_kaitai.NewExprCalcArrayOps()
	err = reparsed.Read(stream2, reparsed, reparsed)
	require.NoError(t, err)

	// Compare structures with Phase 2/3 enhanced comparison
	cmpOpts := []cmp.Option{
		cmpopts.IgnoreUnexported(
			kaitai.Stream{},
		),
		cmpopts.IgnoreFields(expr_calc_array_ops_kaitai.ExprCalcArrayOps{}, "_io", "_parent", "_root"),
		cmpopts.EquateEmpty(),
	}
	if diff := cmp.Diff(original, reparsed, cmpOpts...); diff != "" {
		t.Errorf("Serialization mismatch (-original +reparsed):\n%s", diff)
	}
}

// structToMapExprCalcArrayOps converts KSC struct to map for serializer
func structToMapExprCalcArrayOps(t *testing.T, data any) map[string]any {
	// Use reflection to handle KSC structs with method-based values
	return structToMapReflectiveExprCalcArrayOps(t, data)
}

// structToMapReflectiveExprCalcArrayOps uses reflection to convert KSC structs including method calls
func structToMapReflectiveExprCalcArrayOps(t *testing.T, data any) map[string]any {
	result := make(map[string]any)

	val := reflect.ValueOf(data)
	if val.Kind() == reflect.Ptr {
		val = val.Elem()
	}

	typ := val.Type()

	// Process fields
	for i := 0; i < val.NumField(); i++ {
		field := typ.Field(i)
		fieldVal := val.Field(i)

		// Skip internal KSC fields
		if strings.HasPrefix(field.Name, "_") {
			continue
		}

		fieldName := strings.ToLower(field.Name)

		if !fieldVal.IsValid() || fieldVal.IsZero() {
			continue
		}

		// Handle different field types
		switch fieldVal.Kind() {
		case reflect.Ptr:
			if !fieldVal.IsNil() {
				// For custom types, check for AsInt/AsStr methods
				subResult := make(map[string]any)

				// Try to call AsInt method
				if method := fieldVal.MethodByName("AsInt"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asint"] = results[0].Interface()
					}
				}

				// Try to call AsStr method
				if method := fieldVal.MethodByName("AsStr"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asstr"] = results[0].Interface()
					}
				}

				// Also include raw fields
				subMap := structToMapReflectiveExprCalcArrayOps(t, fieldVal.Interface())
				for k, v := range subMap {
					if _, exists := subResult[k]; !exists {
						subResult[k] = v
					}
				}

				result[fieldName] = subResult
			}
		case reflect.Slice:
			if fieldVal.Len() > 0 {
				slice := make([]any, fieldVal.Len())
				for j := 0; j < fieldVal.Len(); j++ {
					slice[j] = fieldVal.Index(j).Interface()
				}
				result[fieldName] = slice
			}
		default:
			result[fieldName] = fieldVal.Interface()
		}
	}

	return result
}
