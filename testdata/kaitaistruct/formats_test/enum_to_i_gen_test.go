// Code generated by kaitai-test-gen-simple.go; DO NOT EDIT.
package formats_test

import (
	"bytes"
	"context"
	"log/slog"
	"maps"
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/google/go-cmp/cmp/cmpopts"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/twinfer/kbin-plugin/pkg/kaitaistruct"
	enum_to_i_kaitai "github.com/twinfer/kbin-plugin/testdata/formats_kaitai_go_gen/enum_to_i"
)

func TestParse_EnumToI(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "enum_to_i.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)
	logger := slog.New(slog.NewTextHandler(os.Stderr, &slog.HandlerOptions{Level: slog.LevelDebug}))
	interpreter, err := kaitaistruct.NewKaitaiInterpreter(schema, logger)
	require.NoError(t, err)

	// Read binary file
	binPath := filepath.Join("../../../test/src", "enum_0.bin")
	binData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse with custom parser
	stream := kaitai.NewStream(bytes.NewReader(binData))
	parsed, err := interpreter.Parse(context.Background(), stream)
	require.NoError(t, err)

	// Convert to map for assertions
	customMap := kaitaistruct.ParsedDataToMap(parsed).(map[string]any)

	// Assertions from KSC test

	// Assert Pet1
	if pet1_val, ok := customMap["Pet1"]; ok {
		assert.EqualValues(t, EnumToI_Animal__Cat, pet1_val)
	} else {
		t.Fatalf("Field 'Pet1' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet2
	if pet2_val, ok := customMap["Pet2"]; ok {
		assert.EqualValues(t, EnumToI_Animal__Chicken, pet2_val)
	} else {
		t.Fatalf("Field 'Pet2' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet1I
	if pet1i_val, ok := customMap["Pet1I"]; ok {
		assert.EqualValues(t, 7, pet1i_val)
	} else {
		t.Fatalf("Field 'Pet1I' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet1IToS
	if pet1itos_val, ok := customMap["Pet1IToS"]; ok {
		assert.EqualValues(t, "7", pet1itos_val)
	} else {
		t.Fatalf("Field 'Pet1IToS' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet1Mod
	if pet1mod_val, ok := customMap["Pet1Mod"]; ok {
		assert.EqualValues(t, 32775, pet1mod_val)
	} else {
		t.Fatalf("Field 'Pet1Mod' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert OneLtTwo
	if onelttwo_val, ok := customMap["OneLtTwo"]; ok {
		assert.EqualValues(t, true, onelttwo_val)
	} else {
		t.Fatalf("Field 'OneLtTwo' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet1EqInt
	if pet1eqint_val, ok := customMap["Pet1EqInt"]; ok {
		assert.EqualValues(t, true, pet1eqint_val)
	} else {
		t.Fatalf("Field 'Pet1EqInt' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

	// Assert Pet2EqInt
	if pet2eqint_val, ok := customMap["Pet2EqInt"]; ok {
		assert.EqualValues(t, false, pet2eqint_val)
	} else {
		t.Fatalf("Field 'Pet2EqInt' not found in customMap (keys: %v)", maps.Keys(customMap))
	}

}

func TestSerialize_EnumToI(t *testing.T) {
	// Load schema
	ksyPath := filepath.Join("../../../test/formats", "enum_to_i.ksy")
	yamlData, err := os.ReadFile(ksyPath)
	require.NoError(t, err)

	schema, err := kaitaistruct.NewKaitaiSchemaFromYAML(yamlData)
	require.NoError(t, err)

	serializer, err := kaitaistruct.NewKaitaiSerializer(schema, nil)
	require.NoError(t, err)

	// Read original binary
	binPath := filepath.Join("../../../test/src", "enum_0.bin")
	originalData, err := os.ReadFile(binPath)
	require.NoError(t, err)

	// Parse original
	stream := kaitai.NewStream(bytes.NewReader(originalData))
	original := enum_to_i_kaitai.NewEnumToI()
	err = original.Read(stream, original, original)
	require.NoError(t, err)

	// Serialize and verify round-trip
	inputMap := structToMapEnumToI(t, original)

	serialized, err := serializer.Serialize(context.Background(), inputMap)
	require.NoError(t, err)

	// Parse serialized data
	stream2 := kaitai.NewStream(bytes.NewReader(serialized))
	reparsed := enum_to_i_kaitai.NewEnumToI()
	err = reparsed.Read(stream2, reparsed, reparsed)
	require.NoError(t, err)

	// Compare structures with Phase 2/3 enhanced comparison
	cmpOpts := []cmp.Option{
		cmpopts.IgnoreUnexported(
			kaitai.Stream{},
		),
		cmpopts.IgnoreFields(enum_to_i_kaitai.EnumToI{}, "_io", "_parent", "_root"),
		cmpopts.EquateEmpty(),
	}
	if diff := cmp.Diff(original, reparsed, cmpOpts...); diff != "" {
		t.Errorf("Serialization mismatch (-original +reparsed):\n%s", diff)
	}
}

// structToMapEnumToI converts KSC struct to map for serializer
func structToMapEnumToI(t *testing.T, data any) map[string]any {
	// Use reflection to handle KSC structs with method-based values
	return structToMapReflectiveEnumToI(t, data)
}

// structToMapReflectiveEnumToI uses reflection to convert KSC structs including method calls
func structToMapReflectiveEnumToI(t *testing.T, data any) map[string]any {
	result := make(map[string]any)

	val := reflect.ValueOf(data)
	if val.Kind() == reflect.Ptr {
		val = val.Elem()
	}

	typ := val.Type()

	// Process fields
	for i := 0; i < val.NumField(); i++ {
		field := typ.Field(i)
		fieldVal := val.Field(i)

		// Skip internal KSC fields
		if strings.HasPrefix(field.Name, "_") {
			continue
		}

		fieldName := strings.ToLower(field.Name)

		if !fieldVal.IsValid() || fieldVal.IsZero() {
			continue
		}

		// Handle different field types
		switch fieldVal.Kind() {
		case reflect.Ptr:
			if !fieldVal.IsNil() {
				// For custom types, check for AsInt/AsStr methods
				subResult := make(map[string]any)

				// Try to call AsInt method
				if method := fieldVal.MethodByName("AsInt"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asint"] = results[0].Interface()
					}
				}

				// Try to call AsStr method
				if method := fieldVal.MethodByName("AsStr"); method.IsValid() {
					if results := method.Call(nil); len(results) == 2 && results[1].IsNil() {
						subResult["asstr"] = results[0].Interface()
					}
				}

				// Also include raw fields
				subMap := structToMapReflectiveEnumToI(t, fieldVal.Interface())
				for k, v := range subMap {
					if _, exists := subResult[k]; !exists {
						subResult[k] = v
					}
				}

				result[fieldName] = subResult
			}
		case reflect.Slice:
			if fieldVal.Len() > 0 {
				slice := make([]any, fieldVal.Len())
				for j := 0; j < fieldVal.Len(); j++ {
					slice[j] = fieldVal.Index(j).Interface()
				}
				result[fieldName] = slice
			}
		default:
			result[fieldName] = fieldVal.Interface()
		}
	}

	return result
}
