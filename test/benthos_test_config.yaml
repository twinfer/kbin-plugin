input:
  file:
    paths: ["testdata/formats/png/samples/*.png"]
    codec: raw

pipeline:
  processors:
    - kaitai:
        schema_path: "testdata/formats/png/png.ksy"
        is_parser: true

output:
  file:
    path: "test/output.json"
    codec: json
